/*
    int colX = 6, colY = 7;
    mapImageToDisplay = createImage(1080, 540, RGB); //map size is 1080 * 540 as image
    mapImageToDisplay.loadPixels();

    for ( int i = 0; i < mapHits.size(); i++) {
      if ( floatColumns[10][mapHits.get(i)] == 1) {
        mapImageToDisplay.pixels[i] = LAND;
      } else {
        mapImageToDisplay.pixels[i] = SEA;
      }
    }
    mapImageToDisplay.updatePixels();
    toMakeImage = 1;
*/
